/**
 * Тестовый файл
 * 
 * Здесь можно разместить тесты для вашего приложения
 */

function testFunction() {
  // Здесь можно написать тесты
  console.log('Запуск тестов...');
  
  // Пример простого теста
  const expected = true;
  const actual = true;
  
  if (expected === actual) {
    console.log('✅ Тест пройден');
  } else {
    console.error('❌ Тест не пройден');
  }
}

testFunction();
